% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoencoder.R
\name{tidy_autoencoder}
\alias{tidy_autoencoder}
\title{Autoencoder for Dimensionality Reduction}
\usage{
tidy_autoencoder(
  data,
  encoding_dim = 2,
  hidden_layers = c(64, 32),
  activation = "Tanh",
  epochs = 100,
  learning_rate = 0.001,
  scale = TRUE,
  seed = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame or tibble containing the data to analyze}

\item{encoding_dim}{Dimension of the encoding layer (bottleneck). Default is 2}

\item{hidden_layers}{Vector specifying the number of units in hidden layers
between input and encoding. Default is c(64, 32). The decoder mirrors this.}

\item{activation}{Activation function for hidden layers. Options are "Rectifier",
"Tanh", "Maxout", "ExpRectifier". Default is "Tanh"}

\item{epochs}{Number of training epochs. Default is 100}

\item{learning_rate}{Learning rate for optimizer. Default is 0.001}

\item{scale}{Logical; if TRUE, scales variables to the range [0, 1]. Default is TRUE}

\item{seed}{Random seed for reproducibility. Default is NULL}

\item{...}{Additional arguments passed to h2o.deeplearning()}
}
\value{
A list containing:
\item{encoding}{Tibble with the low-dimensional encoding}
\item{reconstruction}{Tibble with reconstructed data}
\item{model}{The trained H2O autoencoder model}
\item{reconstruction_error}{Mean squared error of reconstruction}
\item{params}{List of parameters used}
\item{call}{The matched call}
}
\description{
Trains a neural network autoencoder for nonlinear dimensionality reduction using H2O.
An autoencoder learns to compress data into a lower-dimensional representation
(encoding) and then reconstruct it (decoding).
}
\examples{
\dontrun{
library(tidyul)
library(dplyr)

# Example with iris data
iris_numeric <- iris \%>\% select(where(is.numeric))
ae_result <- tidy_autoencoder(
  iris_numeric,
  encoding_dim = 2,
  hidden_layers = c(8, 4),
  epochs = 50
)

# View encoding
head(ae_result$encoding)

# Check reconstruction error
ae_result$reconstruction_error

# Plot encoding
library(ggplot2)
ae_result$encoding \%>\%
  mutate(species = iris$Species) \%>\%
  ggplot(aes(x = Dim1, y = Dim2, color = species)) +
  geom_point(size = 3)
}

}
