% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_silhouette_classic}
\alias{plot_silhouette_classic}
\title{Plot Classic Silhouette Diagram}
\usage{
plot_silhouette_classic(
  sil_obj = NULL,
  clusters = NULL,
  dist_mat = NULL,
  title = "Silhouette Plot - Cluster Assignment Quality",
  colors = NULL,
  border = NA
)
}
\arguments{
\item{sil_obj}{Either a tidy_silhouette object, a silhouette object from cluster package,
or NULL (if providing clusters and dist_mat)}

\item{clusters}{Optional vector of cluster assignments (used if sil_obj is NULL)}

\item{dist_mat}{Optional distance matrix (used if sil_obj is NULL)}

\item{title}{Plot title (default: "Silhouette Plot - Cluster Assignment Quality")}

\item{colors}{Vector of colors for clusters (default: uses standard palette)}

\item{border}{Color for bar borders (default: NA for no border)}
}
\value{
A ggplot object
}
\description{
Create a classic silhouette plot similar to base R's plot.silhouette(),
showing individual silhouette widths for each observation grouped by cluster,
with a dashed vertical line indicating the average silhouette width.
}
\examples{
\dontrun{
# Using tidy_silhouette object
sil_obj <- tidy_silhouette(clusters, dist_mat)
plot_silhouette_classic(sil_obj)

# Using clusters and distance matrix directly
plot_silhouette_classic(clusters = my_clusters, dist_mat = my_dist)

# Using base R silhouette object
sil <- cluster::silhouette(my_clusters, my_dist)
plot_silhouette_classic(sil)
}
}
