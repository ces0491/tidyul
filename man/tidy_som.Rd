% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/som.R
\name{tidy_som}
\alias{tidy_som}
\title{Self-Organizing Map (SOM)}
\usage{
tidy_som(
  data,
  grid_dim = c(10, 10),
  rlen = 100,
  alpha = c(0.05, 0.01),
  radius = 1,
  dist_fct = "euclidean",
  toroidal = FALSE,
  keep = FALSE,
  ...
)
}
\arguments{
\item{data}{A data frame or tibble containing the data to analyze}

\item{grid_dim}{A numeric vector of length 2 specifying grid dimensions (rows, cols).
Default is c(10, 10)}

\item{rlen}{Number of training iterations. Default is 100}

\item{alpha}{Learning rate, a vector of two numbers indicating the amount of change.
Default is c(0.05, 0.01)}

\item{radius}{Neighborhood radius. Default is 1}

\item{dist_fct}{Distance function. Options are "sumofsquares", "euclidean", "manhattan".
Default is "euclidean"}

\item{toroidal}{Logical; if TRUE, the grid is toroidal (edges wrap around).
Default is FALSE}

\item{keep}{Logical; if TRUE, keeps changes and intermediate results.
Default is FALSE}

\item{...}{Additional arguments passed to kohonen::som()}
}
\value{
A list containing:
\item{model}{The fitted SOM model (kohonen object)}
\item{mapping}{Tibble with original data and unit assignments}
\item{codes}{Tibble with codebook vectors for each unit}
\item{distances}{Matrix of distances between units}
\item{grid}{Information about the SOM grid}
}
\description{
Performs Self-Organizing Map (Kohonen network) analysis on a dataset.
SOMs are unsupervised neural networks that produce low-dimensional representations
while preserving topological properties of the input data.
}
\examples{
\dontrun{
library(tidyul)
library(dplyr)

# Example with iris data
iris_numeric <- iris \%>\% select(where(is.numeric))
som_result <- tidy_som(iris_numeric, grid_dim = c(5, 5), rlen = 200)

# View mapping
head(som_result$mapping)

# Plot SOM
plot(som_result)
}

}
